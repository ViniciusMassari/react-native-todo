import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    height: 327,
    marginTop: 21,
    paddingTop: 48,
    borderTopWidth: 1,
  },
  infoText: {
    textAlign: 'center',
    marginTop: 16,
  },
});
