import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    fontSize: 14,
    borderRadius: 10,
    marginBottom: 8,
  },
  checkBoxContainer: {
    backgroundColor: 'transparent',
  },
  deleteIcon: {
    marginEnd: 17,
  },
});
